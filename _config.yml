plugins:
    - jekyll-autoprefixer

exclude:
    - README.md
    - Makefile
    - Dockerfile
    - Jenkinsfile
    
collections_dir: collections

collections:
    en:
        output: true
        permalink: /en/:name
        naaame: A
    fr:
        output: true
        permalink: /fr/:name
        naaame: B
    es:
        output: true
        permalink: /es/:name
        naaame: C

defaults:
    - scope:
        path: _posts
      values:
        layout: post

sass:
    style: compressed

compress_html:
    clippings: all
    comments: ["<!-- ", " -->"]
    endings: all
    ignore:
        envs: [local]
    blanklines: false
    startings: [html, head, body]
