plugins:
    - jekyll-autoprefixer

kramdown:
    parse_block_html: true

exclude:
    - README.md
    - Makefile

compress_html:
    clippings: all
    comments: ["<!-- ", " -->"]
    endings: all
    ignore:
        envs: [local]
    blanklines: false
    startings: [html, head, body]